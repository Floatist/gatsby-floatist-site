import * as React from 'react'
import Layout from '../components/layout'
import Alert from 'react-bootstrap/Alert'

const PricingPage = () => {

    return (
        <Layout pageTitle="Pricing">
            <p>Pricing</p>
            <p>These are our prices</p>
        </Layout>
    )
}

export default PricingPage